{% use 'bootstrap_3_horizontal_layout.html.twig' %}

{# Add hidden field with component signal and submit button. #}
{%- block form_end -%}
	<div class="form-group">
		<div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2">
			<input type="submit" value="Uložit" class="btn btn-primary">
		</div>
	</div>
	<input type="hidden" name="do" value="{{ component }}-submit">
    {{- parent() -}}
{%- endblock form_end -%}

{# Add form-horizontal class to form tag, enable ajax validation. #}
{% block form_start -%}
	{% set attr = attr|merge({class: (attr.class|default('') ~ 'form-ajax-validation')|trim, 'data-validate': component ~ '-validate'}) %}
	{{- parent() -}}
{%- endblock %}

{# Adjust responsive classes. #}
{% block form_label_class -%}
col-sm-3 col-lg-2
{%- endblock form_label_class %}

{% block form_group_class -%}
col-sm-9 col-lg-10
{%- endblock form_group_class %}

{# Wrap errors by a placeholder element. #}
{% block form_errors -%}
	<div id="error__{{ id }}" class="error-container">
		{{- block('form_errors_content') -}}
	</div>
{%- endblock form_errors %}

{# Only show one error for each field. #}
{% block form_errors_content -%}
	{% if errors|length > 0 -%}
		{%- if form.parent -%}
			<span class="error-message">{{ errors[0].message }}</span>
		{%- else -%}
			<div class="alert alert-danger">
				<i class="fa fa-ban"></i>
				{{ errors[0].message }}
			</div>
		{%- endif -%}
	{%- endif -%}
{%- endblock form_errors_content %}

{% block url_widget -%}
	<div class="input-group">
		<span class="input-group-addon fa fa-internet-explorer"></span>
		{{- parent() -}}
	</div>
{%- endblock url_widget %}

{% block date_widget -%}
	{%- if widget == 'single_text' -%}
		<div class="input-group">
			<span class="input-group-addon fa fa-calendar"></span>
			{{- parent() -}}
		</div>
	{%- else -%}
		{{- parent() -}}
	{%- endif -%}
{%- endblock date_widget %}

{% block datetime_widget -%}
	{%- if widget == 'single_text' -%}
		<div class="input-group">
			<span class="input-group-addon fa fa-calendar"></span>
			{{- parent() -}}
		</div>
	{%- else -%}
		{{- parent() -}}
	{%- endif -%}
{%- endblock datetime_widget %}

{% block time_widget -%}
	{%- if widget == 'single_text' -%}
		<div class="input-group">
			<span class="input-group-addon fa fa-clock-o"></span>
			{{- block('form_widget_simple') -}}
		</div>
	{%- else -%}
        {% set attr = attr|merge({class: (attr.class|default('') ~ ' form-inline')|trim}) -%}
        {% if datetime is not defined or false == datetime -%}
            <div {{ block('widget_container_attributes') -}}>
        {%- endif -%}
        {{- form_widget(form.hour) }} : {{ form_widget(form.minute) }}{% if with_seconds %} : {{ form_widget(form.second) }}{% endif %}
        {% if datetime is not defined or false == datetime -%}
            </div>
        {%- endif -%}
	{%- endif -%}
{%- endblock time_widget %}

{% block email_widget -%}
	<div class="input-group">
		<span class="input-group-addon fa fa-at"></span>
		{{- parent() -}}
	</div>
{%- endblock email_widget %}

{% block country_widget -%}
	<div class="input-group">
		<span class="input-group-addon fa fa-globe"></span>
		{{ block('choice_widget') }}
	</div>
{%- endblock country_widget %}

{% block language_widget -%}
	<div class="input-group">
		<span class="input-group-addon fa fa-language"></span>
		{{ block('choice_widget') }}
	</div>
{%- endblock language_widget %}

{% block locale_widget -%}
	<div class="input-group">
		<span class="input-group-addon fa fa-map-marker"></span>
		{{ block('choice_widget') }}
	</div>
{%- endblock locale_widget %}

{% block timezone_widget -%}
	<div class="input-group">
		<span class="input-group-addon fa fa-clock-o"></span>
		{{ block('choice_widget') }}
	</div>
{%- endblock timezone_widget %}

{% block currency_widget -%}
	<div class="input-group">
		<span class="input-group-addon fa fa-money"></span>
		{{ block('choice_widget') }}
	</div>
{%- endblock currency_widget %}
